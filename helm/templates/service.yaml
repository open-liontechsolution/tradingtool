apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradingtools.fullname" . }}
  labels:
    {{- include "tradingtools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "tradingtools.selectorLabels" . | nindent 4 }}
